# VEE-REAL サービス改修方針

## 優先度1: 軽量・高速化

### 現状の課題
- jQuery + Fancybox を CDN から読み込んでいる（合計 ~90KB gzip）
- 画像はオリジナルサイズのまま配信（リサイズ・フォーマット変換なし）
- 全投稿を一度に取得して返している（LIMIT 60 固定）

### 改善方針
- **jQuery / Fancybox を削除** — ライトボックスが必要なら素のJS or 軽量ライブラリ（< 5KB）に置換
- **画像の最適化** — アップロード時に WebP 変換 + リサイズ（フィード用サムネイル / 詳細用の2段階）
- **レスポンスサイズ削減** — 不要なインラインスタイル・スクリプトの排除、CSS の最小化
- **キャッシュヘッダー** — 静的アセットに Cache-Control を設定

## 優先度2: 縦の無限スクロール

### 現状の課題
- フィードは全件一括レンダリング（SSR で `<ol>` に全件展開）
- ページネーションなし

### 改善方針
- **カーソルベースのページネーション API** — `GET /api/posts?cursor=<created_at>&limit=20` を新設
- **クライアント側の Intersection Observer** — 最下部に到達したら次ページを fetch して DOM に追加
- **初回 SSR + 追加分は JSON → クライアント描画** のハイブリッド構成
- スクロール位置の復元（戻るボタン対応）を考慮

## 優先度3: 広告の導入

### 方針
- **フィード内ネイティブ広告** — 投稿カードと同じレイアウトで数件おきに広告を挿入（ユーザー体験を損なわない配置）
- **無限スクロールとの統合** — ページ読み込みごとに広告枠を動的に挿入（Intersection Observer と連携）
- **広告プロバイダ** — Google AdSense or 軽量な自前バナー配信から開始
- **パフォーマンスへの配慮** — 広告スクリプトは遅延読み込み（lazy load）、LCP への悪影響を避ける
- **表示ルール** — 投稿 5件ごとに1枠程度、画面上に同時に2枠以上表示しない
- **有料プランユーザーには非表示** — ユーザーのプラン情報を参照し、広告挿入をスキップ

## 優先度4: 有料プラン（モック実装）

### 方針
- **プラン種別** — Free（デフォルト）/ Pro（有料）の2段階
- **Pro の特典** — 広告非表示
- **データモデル** — users テーブルに `plan` カラム追加（`'free' | 'pro'`、デフォルト `'free'`）
- **決済はモック** — 実際の課金処理は行わず、プラン変更ボタンを押すだけで Pro に切り替わる
- **UI** — 設定ページ or ヘッダーに現在のプランを表示、切り替えボタンを配置
- **サーバー側の判定** — フィード描画時に `user.plan` を参照し、Free なら広告を挿入、Pro ならスキップ

## 優先度5: カメラ撮影による投稿

### 現状の課題
- 投稿フォームの `<input type="file">` はファイル選択のみ（モバイルでもカメラ直接起動が保証されない）

### 改善方針
- **input の accept / capture 属性追加** — `accept="image/*" capture="environment"` でモバイルのカメラを直接起動可能にする
- **デスクトップ向け: WebRTC カメラUI** — `navigator.mediaDevices.getUserMedia()` でカメラプレビューを表示し、キャプチャボタンで撮影
- **UIの切り替え** — 「ファイルを選択」と「カメラで撮影」の2つのモードを提供
- **撮影後のフロー** — プレビュー表示 → 確認 → 既存のアップロード処理に合流（サーバー側変更不要）

## 優先度6: マイページ（自分の投稿一覧）

### 現状の課題
- 自分の過去の投稿だけを確認する手段がない（フィードは全ユーザーの投稿が混在）

### 改善方針
- **マイページルート追加** — `GET /mypage` でログインユーザー自身の投稿のみ表示
- **クエリ** — `WHERE user_id = ?` で絞り込み、新しい順に表示
- **UI** — ヘッダーにマイページへのリンクを追加（ログイン時のみ表示）
- **無限スクロール対応** — フィードと同様のカーソルベースページネーションを適用
- **投稿の削除** — マイページから自分の投稿を削除できるボタンを配置
